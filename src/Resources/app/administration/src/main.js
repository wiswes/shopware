// Entry point that Shopware's admin build picks up. Every plugin's
// `Resources/app/administration/src/main.js` is auto-loaded into the
// admin SPA after running `bin/build-administration.sh`.
import './module/wiswes-widget';
